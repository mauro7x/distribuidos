# Paths
CONFIG_DIRPATH = '/config'
MIDDLEWARE_CONFIG_FILEPATH = f'{CONFIG_DIRPATH}/middleware.json'
COMMON_CONFIG_FILEPATH = f'{CONFIG_DIRPATH}/common.json'

# Constant messages
EOF_MSG_ID = 'EOF'
EOF_MSG = EOF_MSG_ID

# Serialization
MSG_SEP = ' '
